# Python Version: 3.11.9
certifi==2026.7.22
cryptography==50.0.1
ddddocr==1.5.6
idna==3.20
looseversion==1.3.0
zendriver==0.16.0
numpy==2.2.6
Pillow==12.3.0
playsound==1.2.2
pyperclip==1.11.0
tornado==6.5.10
urllib3==2.8.0
requests==2.34.2
onnxruntime==1.23.1
# Pinned although only ddddocr pulls it in: unpinned, CI resolved to 5.0 while
# every local build had been verified on 4.12, on both Windows and macOS.
opencv-python-headless==4.12.0.88

# cv2 comes from ddddocr's opencv-python-headless. Do not add opencv-python
# here: both ship the same cv2 namespace, and having both installed is a known
# PyInstaller packaging hazard. No project code imports cv2 directly.
# chardet was also dropped -- nothing imports it and no dependency requires it.
