---
allowed-tools: Bash(gh issue view:*), Bash(gh search:*), Bash(gh issue list:*), Bash(gh api:*), Bash(gh issue comment:*)
description: 尋找重複的 GitHub issues
model: haiku
---

# 尋找重複 Issues

針對指定的 GitHub issue，尋找最多 3 個可能重複的 issues。

## 執行步驟

按照以下步驟精確執行：

1. **驗證檢查**：使用 agent 檢查此 issue 是否：
   - (a) 已關閉
   - (b) 不需要去重（例如：廣泛的產品回饋、正面評價、教學請求）
   - (c) 已有重複標記的留言
   - 若符合以上任一條件，**不繼續執行**

2. **摘要 Issue**：使用 agent 查看 issue 內容，並返回問題摘要
   - 包含：平台名稱、錯誤類型、關鍵字

3. **平行搜尋**：啟動 5 個平行 agents，使用不同關鍵字和搜尋方式尋找重複 issues
   - 使用步驟 2 的摘要
   - 搜尋關鍵字應包含：平台、錯誤訊息片段、功能模組
   - 範例：`"TixCraft SID"`, `"NoDriver 登入"`, `"AttributeError to_json"`, `"KKTIX crash"`

4. **過濾誤報**：將步驟 2 和 3 的結果交給另一個 agent，過濾掉不太可能是重複的 issues
   - 若無重複 issues 剩餘，**不繼續執行**

5. **留言並標記**：在原 issue 留言，列出最多 3 個重複 issues（若無則為 0）
   - **判斷使用者語言**：根據 issue 的 title 和 body 判斷使用繁體中文或英文

---

## 重要規則

**必須告訴所有 agents**：
- 僅使用 `gh` CLI 與 GitHub 互動，不使用 web fetch
- 不使用其他工具（如 MCP servers、file edit 等）
- 開始前先建立 todo list

---

## 留言格式

**根據使用者語言自動選擇範本**（判斷 issue title 和 body 的語言）：

### 繁體中文範本

假設找到 3 個疑似重複的 issues：

```
發現 3 個可能重複的 issues：

1. <issue 連結>
2. <issue 連結>
3. <issue 連結>

此 issue 將在 3 天後自動關閉為重複問題。

- 若你的問題確實重複，請關閉此 issue 並到現有 issue 👍
- 若要防止自動關閉，請留言或對此留言 👎

🤖 Generated with [Claude Code](https://claude.ai/code)
```

### English Template

If 3 suspected duplicates are found:

```
Found 3 possible duplicate issues:

1. <issue link>
2. <issue link>
3. <issue link>

This issue will be automatically closed as a duplicate in 3 days.

- If your issue is a duplicate, please close it and 👍 the existing issue instead
- To prevent auto-closure, add a comment or 👎 this comment

🤖 Generated with [Claude Code](https://claude.ai/code)
```

---

## 語言判斷邏輯

**判斷順序**：
1. 檢查 issue title 是否包含中文字元（繁體/簡體）→ 使用繁體中文範本
2. 檢查 issue body 是否主要為中文 → 使用繁體中文範本
3. 預設使用英文範本

---

## 平台特定搜尋關鍵字（Tickets Hunter 專案）

針對不同平台使用特定關鍵字搜尋：

- **TixCraft**：`"TixCraft"`, `"拓元"`, `"SID"`, `"Google 登入"`, `"驗證碼"`
- **KKTIX**：`"KKTIX"`, `"崩潰"`, `"crash"`, `"場次選擇"`, `"驗證問題"`
- **iBon**：`"iBon"`, `"Shadow DOM"`, `"Angular"`, `"Cookie"`, `"日期選擇"`
- **TicketPlus**：`"TicketPlus"`, `"遠大"`, `"關鍵字"`, `"自動遞補"`, `"價位"`
- **NoDriver**：`"NoDriver"`, `"CDP"`, `"反偵測"`, `"AttributeError"`, `"to_json"`

---

## 不去重的情況

以下類型的 issues 不進行去重：
- 教學請求（如「如何下載」、「影片教學」）
- 功能請求（有 `enhancement` label）
- 空白 issue（會由其他流程處理）
- 正面回饋或產品建議