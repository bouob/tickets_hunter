# TicketPlus 折價券觸發邏輯順序

詳細說明折價券功能的觸發條件與判斷順序

---

## ✅ 答案：先判斷有無輸入，再檢查頁面欄位

### 觸發邏輯順序（從程式碼 Line 6773-6783）

```python
# 讀取折價券代碼設定
discount_code = config_dict["advanced"].get("ticketplus_discount_code", "").strip()

# 如果沒有設定折價券代碼，直接跳過
if not discount_code:
    if show_debug_message:
        print("[DISCOUNT CODE] No discount code configured, skipping")
    return False, fail_list, False

if show_debug_message:
    print(f"[DISCOUNT CODE] Attempting to fill discount code: {discount_code}")

# ... 這裡才開始執行 JavaScript 檢查頁面欄位
```

---

## 📊 完整的判斷流程圖

```
開始執行折價券處理函數
    │
    ▼
┌─────────────────────────────┐
│ 檢查 1: 程式是否暫停？        │
│ if await check_and_handle_   │
│     pause():                 │
└──┬──────────────────────────┘
   │
   ├─ 是 → 立即返回，不處理
   │
   └─ 否
      │
      ▼
┌──────────────────────────────────────┐
│ 檢查 2: 使用者有沒有輸入折價券代碼？   │
│ discount_code = config_dict          │
│     ["advanced"]                     │
│     .get("ticketplus_discount_code") │
│     .strip()                         │
└──┬───────────────────────────────────┘
   │
   ├─ 空字串 → 跳過處理，返回
   │   │
   │   └─ 輸出: "No discount code configured, skipping"
   │
   └─ 有值 → 繼續執行
      │
      ▼
┌──────────────────────────────────────┐
│ 輸出 Debug 訊息                       │
│ print(f"Attempting to fill discount  │
│        code: {discount_code}")       │
└──┬───────────────────────────────────┘
   │
   ▼
┌──────────────────────────────────────┐
│ 檢查 3: 執行 JavaScript               │
│         查找頁面上的折價券欄位         │
│                                      │
│ result = await tab.evaluate(         │
│     JavaScript 程式碼)                │
└──┬───────────────────────────────────┘
   │
   │ (JavaScript 內部邏輯)
   │
   ▼
┌──────────────────────────────────────┐
│ JavaScript: 查找 .exclusive-code      │
│             .label 元素               │
└──┬───────────────────────────────────┘
   │
   ├─ 找不到 → filledCount = 0
   │   │
   │   └─ 返回: { success: false, filledCount: 0 }
   │
   └─ 找到
      │
      ▼
┌──────────────────────────────────────┐
│ JavaScript: 檢查標籤文字是否包含關鍵字│
│             ['序號', '加購', '優惠']  │
└──┬───────────────────────────────────┘
   │
   ├─ 不符合 → filledCount = 0
   │
   └─ 符合
      │
      ▼
┌──────────────────────────────────────┐
│ JavaScript: 填入折價券代碼並觸發事件   │
│             filledCount++             │
└──┬───────────────────────────────────┘
   │
   ▼
┌──────────────────────────────────────┐
│ JavaScript: 返回結果                  │
│ return {                             │
│     success: filledCount > 0,        │
│     filledCount: filledCount         │
│ }                                    │
└──┬───────────────────────────────────┘
   │
   ▼
┌──────────────────────────────────────┐
│ Python: 處理返回結果                  │
│                                      │
│ if success and filled_count > 0:     │
│     輸出: "Successfully filled N      │
│            discount code field(s)"   │
│ else:                                │
│     輸出: "No matching discount code  │
│            fields found on page"     │
└──────────────────────────────────────┘
```

---

## 🎯 為什麼先判斷有無輸入？

### 效能優化（Early Return Pattern）

```python
# 如果沒有設定折價券代碼，直接跳過
if not discount_code:
    return False, fail_list, False
```

**優點**：
1. ✅ **避免不必要的 JavaScript 執行**
   - 如果使用者沒有輸入代碼，根本不需要查找頁面元素
   - 節省執行時間（JavaScript 注入有開銷）

2. ✅ **程式邏輯清晰**
   - 先檢查「是否需要處理」
   - 再執行「如何處理」

3. ✅ **減少錯誤風險**
   - 如果代碼為空但仍執行 JavaScript，會浪費資源
   - 可能產生不必要的 Debug 訊息

---

## 📝 實際執行範例

### 情境 1：使用者未輸入折價券

```
settings.json: "ticketplus_discount_code": ""

執行流程：
1. discount_code = ""
2. if not discount_code: ← 條件成立
3. print("No discount code configured, skipping")
4. return False, fail_list, False
5. 🚫 JavaScript 完全不執行
6. 🚫 不查找頁面元素
```

**效能影響**：
- ⚡ 極快（<1ms）
- ⚡ 不消耗瀏覽器資源

---

### 情境 2：使用者有輸入，頁面有欄位

```
settings.json: "ticketplus_discount_code": "TEST123"
頁面 HTML: <div class="exclusive-code"><div class="label">優惠序號</div>...</div>

執行流程：
1. discount_code = "TEST123"
2. if not discount_code: ← 條件不成立，繼續
3. print("Attempting to fill discount code: TEST123")
4. 執行 JavaScript
5. JavaScript 查找 .exclusive-code .label
6. 找到元素，檢查關鍵字 ✅
7. 填入 "TEST123"
8. filledCount = 1
9. return { success: true, filledCount: 1 }
10. print("Successfully filled 1 discount code field(s)")
```

**效能影響**：
- ⏱️ 中等速度（10-50ms）
- 🔍 需要 DOM 查詢

---

### 情境 3：使用者有輸入，但頁面無欄位

```
settings.json: "ticketplus_discount_code": "TEST123"
頁面 HTML: (無折價券欄位)

執行流程：
1. discount_code = "TEST123"
2. if not discount_code: ← 條件不成立，繼續
3. print("Attempting to fill discount code: TEST123")
4. 執行 JavaScript
5. JavaScript 查找 .exclusive-code .label
6. 找不到任何元素 ❌
7. filledCount = 0
8. return { success: false, filledCount: 0 }
9. print("No matching discount code fields found on page")
10. 不影響後續流程，繼續提交表單
```

**效能影響**：
- ⏱️ 中等速度（5-20ms）
- 🔍 執行了 DOM 查詢但無匹配

---

## 🤔 如果改成「先檢查頁面，再檢查輸入」會怎樣？

### ❌ 不推薦的做法

```python
# 假設改成這樣（不推薦）
result = await tab.evaluate('...')  # 先執行 JavaScript
discount_code = config_dict["advanced"].get("ticketplus_discount_code", "")

if not discount_code:
    return False, fail_list, False
```

**問題**：
1. ❌ **浪費資源**
   - 即使使用者沒輸入代碼，也會執行 JavaScript
   - 每次進入頁面都會查找 DOM 元素（沒必要）

2. ❌ **程式邏輯混亂**
   - 先做事，再判斷要不要做
   - 違反 Early Return 原則

3. ❌ **錯誤訊息誤導**
   - 如果找到欄位但使用者沒輸入，會輸出什麼訊息？
   - 容易混淆

---

## 📊 效能比較

| 情境 | 現有做法（先判斷輸入） | 如果先檢查頁面 |
|------|---------------------|--------------|
| 未輸入代碼 | ⚡ <1ms（立即返回） | ⏱️ 10-50ms（執行 JavaScript） |
| 有輸入，有欄位 | ⏱️ 10-50ms | ⏱️ 10-50ms |
| 有輸入，無欄位 | ⏱️ 5-20ms | ⏱️ 5-20ms |

**結論**：現有做法在「未輸入代碼」的情況下明顯更快（這是最常見的情況）。

---

## 🎯 最佳實踐：Early Return Pattern

```python
def process_something(config):
    # 1. 先檢查前置條件（便宜的檢查）
    if not config.enabled:
        return  # 立即返回，不做任何事

    # 2. 檢查必要參數（便宜的檢查）
    value = config.get("value")
    if not value:
        return

    # 3. 執行昂貴的操作（只在必要時）
    result = expensive_operation(value)
    return result
```

**優點**：
- ✅ 效能最佳化
- ✅ 程式碼可讀性高
- ✅ 減少巢狀層級
- ✅ 錯誤處理清晰

---

## 📋 判斷順序總結

### 實際執行順序

```
1. 檢查暫停狀態 (便宜)
   ↓
2. 讀取折價券設定 (便宜)
   ↓
3. 判斷是否有輸入 (便宜)
   ↓ (如果有輸入才執行)
4. 執行 JavaScript (昂貴)
   ↓
5. 查找頁面元素 (昂貴)
   ↓
6. 填入折價券 (昂貴)
```

### 關鍵原則

**便宜的檢查放前面，昂貴的操作放後面**

- ✅ 便宜：讀取變數、字串比較、if 判斷
- ❌ 昂貴：JavaScript 注入、DOM 查詢、網路請求

---

## 🔍 程式碼佐證

### Line 6773-6779（先判斷輸入）

```python
# 讀取折價券代碼設定
discount_code = config_dict["advanced"].get("ticketplus_discount_code", "").strip()

# 如果沒有設定折價券代碼，直接跳過
if not discount_code:
    if show_debug_message:
        print("[DISCOUNT CODE] No discount code configured, skipping")
    return False, fail_list, False
```

**觀察**：
- ✅ 在執行 JavaScript 之前就返回了
- ✅ Line 6789 的 `tab.evaluate()` 完全不會執行

### Line 6789（後檢查頁面）

```python
# 使用 JavaScript 注入填入折價券代碼
result = await tab.evaluate(f'''
    (function() {{
        const keywords = ['序號', '加購', '優惠'];
        ...
    }})()
''')
```

**觀察**：
- ✅ 只有在 `discount_code` 有值時才執行
- ✅ 這是最昂貴的操作，所以放最後

---

## ✅ 總結

| 問題 | 答案 |
|------|------|
| **是先檢查用戶有沒有輸入？** | ✅ 是的 (Line 6776) |
| **還是先檢查頁面有沒有欄位？** | ❌ 不是，這個放後面 (Line 6793) |
| **為什麼這樣設計？** | 效能優化（Early Return Pattern） |
| **如果改成先檢查頁面？** | 會浪費資源，不推薦 |

**邏輯順序**：
```
暫停檢查 → 輸入檢查 → JavaScript執行 → 頁面元素查找 → 填入代碼
(便宜)     (便宜)      (昂貴)        (昂貴)          (昂貴)
```

這樣的設計確保在大多數情況下（使用者未輸入折價券）能最快速地跳過處理，只在真正需要時才執行昂貴的 DOM 操作。
